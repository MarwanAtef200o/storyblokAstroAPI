---
import { storyblokEditable } from '@storyblok/astro'

const { blok } = Astro.props
---


<div
  {...storyblokEditable(blok)}
  class='text-center'
>
  <a class='text-2xl text-gray-200' href=`https://localhost:4321/${blok.label}`>{blok.label}</a>
</div>